Config files are examples. It should work generally with CDK20 telescope and ToupTek SkyEye24AC camera.
For different telescope, optics, camera - fine tuning of parameters may be needed.
Darks and Flats are examples. You will need own calibration files.
For different filters (or different camera) new color calibration is needed (white balance and color matrix).